# HIPAA compliance adapter (yci)

This directory holds HIPAA-shaped artifacts for `yci`. The telemetry sanitizer
loads `phi-redaction.rules` when the active customer profile’s
`compliance.regime` is `hipaa`.

See [`yci/CONTRIBUTING.md`](../../../../CONTRIBUTING.md) for the adapter pattern.
