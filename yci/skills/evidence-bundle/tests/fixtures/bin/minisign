#!/usr/bin/env bash
set -euo pipefail

sig_path=""
artifact=""
while [[ $# -gt 0 ]]; do
    case "$1" in
        -x) sig_path="${2:-}"; shift 2 ;;
        -m) artifact="${2:-}"; shift 2 ;;
        *) shift ;;
    esac
done

printf 'fake-minisign signature for %s\n' "${artifact}" > "${sig_path}"
