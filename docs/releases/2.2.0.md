<!-- Template used by bundle-release/scripts/draft-notes.sh. Placeholders ({{...}}) are replaced at generation time. Do not remove or rename them. -->

# Release 2.2.0 — 2026-04-17

## Summary

Adds a full opencode compatibility target (skills, agents, commands, plus an `opencode.json` config and generated `AGENTS.md`) alongside a new `releaser` skill for external-project releases. Also expands the `init` skill to author project docs, templates, and git conventions, normalizes Cursor agent models to `inherit`/`fast`, and wires up pre-commit hooks.

## Added

2f26627 feat(cursor): normalize generated agent models to inherit or fast
8d23ae9 feat(opencode): add opencode target generator (#24)
5f51b14 feat(init): expand skill to author project docs, templates, and git conventions
5945f10 feat(skills): add releaser skill for external-project releases (#23)

## Changed

c935ae2 chore(lint): add pre-commit hooks and resolve existing lint errors
77137d2 chore(init): apply doc trio, GitHub templates, and git conventions

## Removed

None this release.

## Fixed

None this release.

## Validation

- `./scripts/sync.sh` — OK
- `./scripts/validate.sh` — OK
- `python3 -m json.tool .claude-plugin/marketplace.json` — OK
- `python3 -m json.tool ycc/.claude-plugin/plugin.json` — OK

## Upgrade Notes

No action required — this is a purely additive release. Existing `ycc:` (Claude Code), Cursor, and Codex consumers see only new functionality. opencode users can now install the bundle natively from `.opencode-plugin/`.

## Commit Log

2f26627 feat(cursor): normalize generated agent models to inherit or fast
8d23ae9 feat(opencode): add opencode target generator (#24)
c935ae2 chore(lint): add pre-commit hooks and resolve existing lint errors
77137d2 chore(init): apply doc trio, GitHub templates, and git conventions
5f51b14 feat(init): expand skill to author project docs, templates, and git conventions
5945f10 feat(skills): add releaser skill for external-project releases (#23)

---

Previous release: v2.1.0
