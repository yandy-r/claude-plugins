<!-- Template used by bundle-release/scripts/draft-notes.sh. Placeholders ({{...}}) are replaced at generation time. Do not remove or rename them. -->

# Release 2.3.2 — 2026-04-18

## Summary

Patch release refining the `init` skill's agent-rule templates: adds MUST rules for modularity/reuse and single responsibility, relaxes the hard `>500 lines` file cap to a rational `~500 lines` soft cap, and introduces a language-agnostic SHOULD → General best-practices subsection (naming, dead code, dependency hygiene, fail-fast, tests alongside changes).

## Added

65b2be5 feat(init): add modularity and file-size MUST rules to agent templates
6445ceb feat(init): add large-files MUST rule to CLAUDE.md template

## Changed

- `init` skill templates (`CLAUDE.md.tmpl`, `ai-rule.md.tmpl`, `cursor-rule.mdc.tmpl`) gained coding-best-practice MUST rules; the hard `>500 lines` rule was relaxed to a rational `~500 lines` soft cap.
- Root `CLAUDE.md` mirrored to match the new template rules.

## Removed

TODO: none this release

## Fixed

TODO: none this release

## Validation

- `./scripts/sync.sh` — OK
- `./scripts/validate.sh` — OK
- `python3 -m json.tool .claude-plugin/marketplace.json` — OK
- `python3 -m json.tool ycc/.claude-plugin/plugin.json` — OK

## Upgrade Notes

No breaking changes. New rules only affect **new** projects scaffolded via `/ycc:init`. Existing projects that want the updated rules can re-run `/ycc:init --update` to merge the new MUST/SHOULD bullets into their `CLAUDE.md`.

## Commit Log

e937b72 build(bundles): regen after modularity rule addition
65b2be5 feat(init): add modularity and file-size MUST rules to agent templates
385b69a build(bundles): regen after large-files rule addition
6445ceb feat(init): add large-files MUST rule to CLAUDE.md template

---

Previous release: v2.3.1
