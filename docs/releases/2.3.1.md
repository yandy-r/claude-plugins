<!-- Template used by bundle-release/scripts/draft-notes.sh. Placeholders ({{...}}) are replaced at generation time. Do not remove or rename them. -->

# Release 2.3.1 — 2026-04-18

## Summary

Completeness patch for the v2.3.0 `ycc:formatters` skill — adds the matching `/ycc:formatters` slash command so the skill is reachable as a first-class command alongside `/ycc:init`, `/ycc:clean`, and the rest of the bundle.

## Added

- `/ycc:formatters` slash command — thin wrapper around the existing `ycc:formatters` skill. Documents common invocations and the full flag matrix. Skill behavior is unchanged.

## Changed

- None.

## Removed

- None.

## Fixed

- "Skills without commands" inventory gap for `formatters` — resolved (`karpathy-guidelines` is intentional and remains the only entry).

## Validation

- `./scripts/sync.sh` — OK
- `./scripts/validate.sh` — OK
- `python3 -m json.tool .claude-plugin/marketplace.json` — OK
- `python3 -m json.tool ycc/.claude-plugin/plugin.json` — OK

## Upgrade Notes

No action required. The skill was already reachable via direct invocation in v2.3.0; this release just surfaces it in the slash-command UI.

## Commit Log

b1947fd feat(formatters): add /ycc:formatters slash command

---

Previous release: v2.3.0
