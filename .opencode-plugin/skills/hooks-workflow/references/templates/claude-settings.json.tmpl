{
  "_note": "generator template — do not hand-edit. See .opencode-plugin/skills/hooks-workflow/references/support-notes.md.",
  "hooks": {
    "{{EVENT}}": [
      {
        "matcher": "{{MATCHER}}",
        "hooks": [
          { "type": "command", "command": "{{COMMAND}}" }
        ]
      }
    ]
  }
}
