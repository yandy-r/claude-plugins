{
  "name": "ycc",
  "description": "Scripts for YCC",
  "scripts": {
    "lint": "scripts/lint.sh --python --docs --shell",
    "lint:modified": "scripts/lint.sh --modified --python --docs --shell",
    "lint:staged": "scripts/lint.sh --staged --python --docs --shell",
    "lint:unstaged": "scripts/lint.sh --unstaged --python --docs --shell",
    "lint:fix": "scripts/lint.sh --fix --python --docs --shell",
    "lint:fix:modified": "scripts/lint.sh --fix --modified --python --docs --shell",
    "format": "scripts/format.sh --python --docs",
    "format:modified": "scripts/format.sh --modified --python --docs",
    "format:staged": "scripts/format.sh --staged --python --docs",
    "format:unstaged": "scripts/format.sh --unstaged --python --docs",
    "install:all": "./install.sh install --target all --settings --rules --mcp --hooks",
    "install:all:local": "./install.sh install --target all --mode local --settings --rules --mcp --hooks",
    "install:all:repo": "./install.sh install --target all --mode repo --settings --rules --mcp --hooks",
    "install:all:base": "./install.sh install --target all --only base",
    "install:all:settings": "./install.sh install --target all --only settings",
    "install:all:rules": "./install.sh install --target all --only rules",
    "install:all:mcp": "./install.sh install --target all --only mcp",
    "install:claude": "./install.sh install --target claude --settings --rules --mcp --hooks",
    "install:claude:local": "./install.sh install --target claude --mode local --settings --rules --mcp --hooks",
    "install:claude:repo": "./install.sh install --target claude --mode repo --settings --rules --mcp --hooks",
    "install:claude:base": "./install.sh install --target claude --only base",
    "install:claude:base:local": "./install.sh install --target claude --mode local --only base",
    "install:claude:base:repo": "./install.sh install --target claude --mode repo --only base",
    "install:claude:settings": "./install.sh install --target claude --only settings",
    "install:claude:rules": "./install.sh install --target claude --only rules",
    "install:claude:mcp": "./install.sh install --target claude --only mcp",
    "install:claude:hooks": "./install.sh install --target claude --only hooks",
    "install:cursor": "./install.sh install --target cursor --rules --mcp",
    "install:cursor:base": "./install.sh install --target cursor --only base",
    "install:cursor:rules": "./install.sh install --target cursor --only rules",
    "install:cursor:mcp": "./install.sh install --target cursor --only mcp",
    "install:codex": "./install.sh install --target codex --settings --rules",
    "install:codex:local": "./install.sh install --target codex --mode local --settings --rules",
    "install:codex:repo": "./install.sh install --target codex --mode repo --settings --rules",
    "install:codex:base": "./install.sh install --target codex --only base",
    "install:codex:base:local": "./install.sh install --target codex --mode local --only base",
    "install:codex:base:repo": "./install.sh install --target codex --mode repo --only base",
    "install:codex:settings": "./install.sh install --target codex --only settings",
    "install:codex:rules": "./install.sh install --target codex --only rules",
    "install:opencode": "./install.sh install --target opencode --settings --rules",
    "install:opencode:base": "./install.sh install --target opencode --only base",
    "install:opencode:settings": "./install.sh install --target opencode --only settings",
    "install:opencode:rules": "./install.sh install --target opencode --only rules",
    "sync": "./scripts/sync.sh",
    "sync:inventory": "./scripts/sync.sh --only inventory",
    "sync:cursor": "./scripts/sync.sh --only cursor",
    "sync:codex": "./scripts/sync.sh --only codex",
    "sync:opencode": "./scripts/sync.sh --only opencode",
    "validate": "./scripts/validate.sh",
    "validate:inventory": "./scripts/validate.sh --only inventory",
    "validate:cursor": "./scripts/validate.sh --only cursor",
    "validate:codex": "./scripts/validate.sh --only codex",
    "validate:opencode": "./scripts/validate.sh --only opencode",
    "validate:json": "./scripts/validate.sh --only json",
    "prepare": "lefthook install || true"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.0",
    "@commitlint/config-conventional": "^19.6.0",
    "lefthook": "^1.8.0",
    "markdownlint-cli": "^0.42.0",
    "prettier": "^3.3.3"
  }
}
